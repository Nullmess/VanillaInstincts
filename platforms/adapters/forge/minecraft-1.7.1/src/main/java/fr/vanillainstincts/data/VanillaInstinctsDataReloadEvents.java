package fr.vanillainstincts.data;

/**
 * Minecraft 1.15 compatibility marker. Forge 1.15 has no
 * the later Forge reload-listener hook; the data managers keep their built-in defaults.
 */
public final class VanillaInstinctsDataReloadEvents {
    private VanillaInstinctsDataReloadEvents() {
    }
}
